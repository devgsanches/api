# HTTP
PORT=3333
# Comma-separated list of allowed origins for CORS (frontend URL). Use * for any.
CORS_ORIGIN=http://localhost:3000

# Database (matches docker-compose.yml)
DATABASE_URL=postgresql://app:1q2w3e4r@localhost:5432/devclub?schema=public

# Admin auth (the seed creates this user with role ADMIN)
JWT_SECRET=change-me-in-production
ADMIN_EMAIL=admin@devclub.com
ADMIN_PASSWORD=admin123
